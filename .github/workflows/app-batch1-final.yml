name: APP Batch 1 Final Acceptance

on:
  workflow_dispatch:

jobs:
  final-acceptance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/audit-app-batch1-final.mjs
