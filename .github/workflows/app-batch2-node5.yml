name: APP Batch 2 Node 5

on:
  push:
    branches: [main]
    paths:
      - 'apps/mobile/**'
      - 'supabase/migrations/20260816220000_app_batch2_social_graph.sql'
      - 'scripts/audit-app-batch2-node5.mjs'
      - '.github/workflows/app-batch2-node5.yml'
      - 'docs/APP-BATCH-2-NEWS-EXPERIENCE-AND-ENGAGEMENT.md'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.13.0'
      - name: Verify node 5 follow graph and user profiles
        run: node scripts/audit-app-batch2-node5.mjs
