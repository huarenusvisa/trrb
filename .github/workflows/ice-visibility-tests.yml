name: ICE Visibility Tests

on:
  pull_request:
    paths:
      - "admin/ice-review-v2.js"
      - "netlify/functions/ice-review-list-v3.js"
      - "netlify/functions/_shared/ice-review-list.js"
      - "scripts/ice-review-list.test.mjs"
      - "scripts/ice-filter-replies.mjs"
      - "scripts/ice-translate-title-body.mjs"
      - "scripts/ice-original-posts.test.mjs"
      - ".github/workflows/ice-auto-publish.yml"
      - ".github/workflows/ice-visibility-tests.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - name: Syntax checks
        run: |
          node --check admin/ice-review-v2.js
          node --check netlify/functions/ice-review-list-v3.js
          node --check netlify/functions/_shared/ice-review-list.js
          node --check scripts/ice-filter-replies.mjs
          node --check scripts/ice-translate-title-body.mjs
      - name: ICE candidate tests
        run: |
          node --test scripts/ice-review-list.test.mjs
          node --test scripts/ice-original-posts.test.mjs
