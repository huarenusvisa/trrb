name: JOBS-R1 Node 3

on:
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Strict N3 acceptance
        run: node scripts/audit-jobs-r1-node3.mjs
