# Single production deploy gate for the current TRRB product.
name: 生产部署与当前产品验收

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - '_headers'
      - '_redirects'
      - 'netlify.toml'
      - '*.js'
      - '*.css'
      - 'jobs/**'
      - 'niulai/**'
      - 'immigrate/**'
      - 'immigration-judge-approval-rate/**'
      - 'trump/**'
      - 'topic/**'
      - 'netlify/functions/**'
      - 'netlify/edge-functions/**'
      - 'scripts/generate-sitemaps.mjs'
      - 'scripts/split-sitemap-index.mjs'
      - 'scripts/validate-site.mjs'
      - 'scripts/seo-integrity-audit.mjs'
      - '.github/workflows/round16-node9-production-deploy-recovery.yml'

permissions:
  contents: read
  statuses: write

concurrency:
  group: trrb-production-deploy-main
  cancel-in-progress: true

jobs:
  deploy-and-accept:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
      NETLIFY_SITE_ID: b30280d2-b4f8-4a19-9135-862dd9c7171f
      NETLIFY_BUILD_HOOK: ${{ secrets.NETLIFY_BUILD_HOOK }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: 锁定最新 main
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          sleep 20
          git fetch origin main
          git reset --hard origin/main
          DEPLOY_SHA="$(git rev-parse HEAD)"
          echo "DEPLOY_SHA=$DEPLOY_SHA" >> "$GITHUB_ENV"
          echo "deploying_main=$DEPLOY_SHA"

          statuses="$(curl -fsS             -H "Authorization: Bearer $GITHUB_TOKEN"             -H 'Accept: application/vnd.github+json'             "https://api.github.com/repos/$GITHUB_REPOSITORY/commits/$DEPLOY_SHA/status" || true)"
          already_accepted="$(STATUS_JSON="$statuses" node -e '
            const payload = JSON.parse(process.env.STATUS_JSON || "{}");
            const hit = (payload.statuses || []).some((item) =>
              item.context === "trrb/production-accepted" && item.state === "success"
            );
            process.stdout.write(hit ? "true" : "false");
          ')"
          echo "SKIP_DEPLOY=$already_accepted" >> "$GITHUB_ENV"
          echo "already_accepted=$already_accepted"

      - name: 标记生产部署开始
        if: env.SKIP_DEPLOY != 'true'
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl -fsS -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'Content-Type: application/json' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/statuses/$DEPLOY_SHA" \
            -d '{"state":"pending","context":"trrb/production-trigger","description":"Production deployment trigger started"}' >/dev/null

      - name: 发布 Netlify Production
        if: env.SKIP_DEPLOY != 'true'
        id: deploy
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail

          token_valid=false
          deploy_mode='none'
          if [ -n "${NETLIFY_AUTH_TOKEN:-}" ]; then
            token_http=$(curl -sS -o /tmp/netlify-user.json -w '%{http_code}' \
              -H "Authorization: Bearer $NETLIFY_AUTH_TOKEN" \
              -H 'Accept: application/json' \
              'https://api.netlify.com/api/v1/user' || true)
            if [ "$token_http" = "200" ]; then
              token_valid=true
            else
              echo "Netlify token is present but account authentication returned HTTP $token_http; using build hook fallback."
            fi
          fi

          if [ "$token_valid" = "true" ]; then
            deploy_mode='cli'
            echo 'Deploy mode: Netlify CLI direct production deploy via validated repository token'
            npx --yes netlify-cli@latest deploy --prod --context production
          elif [ -n "${NETLIFY_BUILD_HOOK:-}" ]; then
            deploy_mode='build-hook'
            echo 'Deploy mode: Netlify build hook fallback'
            if [[ "$NETLIFY_BUILD_HOOK" == *\?* ]]; then
              hook_url="${NETLIFY_BUILD_HOOK}&trigger_branch=main&clear_cache=true"
            else
              hook_url="${NETLIFY_BUILD_HOOK}?trigger_branch=main&clear_cache=true"
            fi
            curl -fsS -X POST "$hook_url"
          else
            echo 'No valid Netlify auth token and no build hook configured.'
            exit 2
          fi
          echo "DEPLOY_MODE=$deploy_mode" >> "$GITHUB_ENV"

      - name: 发布生产触发结果状态
        if: ${{ always() && env.SKIP_DEPLOY != 'true' }}
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          DEPLOY_OUTCOME: ${{ steps.deploy.outcome }}
        run: |
          set -euo pipefail
          if [ "$DEPLOY_OUTCOME" = "success" ]; then
            state='success'
            description="Production trigger accepted via ${DEPLOY_MODE:-unknown}"
          else
            state='failure'
            description='Production trigger failed before acceptance'
          fi
          payload=$(node -e 'process.stdout.write(JSON.stringify({state:process.argv[1],context:"trrb/production-trigger",description:process.argv[2]}))' "$state" "$description")
          curl -fsS -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'Content-Type: application/json' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/statuses/$DEPLOY_SHA" \
            -d "$payload" >/dev/null

      - name: 停止失败的生产触发
        if: ${{ env.SKIP_DEPLOY != 'true' && steps.deploy.outcome != 'success' }}
        shell: bash
        run: exit 1

      - name: 验收最新 SHA、首页、招聘、专题和 Sitemap
        if: env.SKIP_DEPLOY != 'true'
        shell: bash
        run: |
          set -euo pipefail
          for attempt in $(seq 1 90); do
            stamp=$(date +%s)
            live_sha=$(curl -fsSL --max-time 20 -H 'Cache-Control: no-cache' "https://trrb.net/deploy-version.txt?v=$stamp" 2>/dev/null | tr -d '\r\n[:space:]' || true)
            home=$(curl -fsSL --max-time 20 -H 'Cache-Control: no-cache' "https://trrb.net/?v=$stamp" || true)
            jobs=$(curl -fsSL --max-time 20 -H 'Cache-Control: no-cache' "https://trrb.net/jobs/?v=$stamp" || true)
            jobs_api=$(curl -fsSL --max-time 20 -H 'Cache-Control: no-cache' "https://trrb.net/.netlify/functions/public-home-jobs?limit=6&v=$stamp" || true)
            bundle=$(curl -fsSL --max-time 20 -H 'Cache-Control: no-cache' "https://trrb.net/.netlify/functions/public-home-bundle?v=$stamp" || true)
            trump=$(curl -fsSL --max-time 20 -H 'Cache-Control: no-cache' "https://trrb.net/trump?v=$stamp" || true)
            sitemap=$(curl -fsSL --max-time 20 -H 'Cache-Control: no-cache' "https://trrb.net/sitemap.xml?v=$stamp" || true)
            headers=$(curl -fsSI --max-time 20 -H 'Cache-Control: no-cache' "https://trrb.net/jobs/?v=$stamp" | tr -d '\r' || true)
            sitemap_headers=$(curl -fsSI --max-time 20 -H 'Cache-Control: no-cache' "https://trrb.net/sitemap.xml?v=$stamp" | tr -d '\r' || true)

            sha_ok=0; [ "$live_sha" = "$DEPLOY_SHA" ] && sha_ok=1
            home_jobs=$(printf '%s' "$home" | grep -c '招聘求职' || true)
            false_empty=$(printf '%s' "$home" | grep -c '当前暂无重点新闻' || true)
            jobs_index=$(printf '%s' "$jobs" | grep -ci 'name="robots" content="index,follow' || true)
            jobs_prelaunch=$(printf '%s' "$jobs" | grep -c '上线准备中' || true)
            jobs_api_ok=$(printf '%s' "$jobs_api" | grep -Ec '"items"[[:space:]]*:[[:space:]]*\[[^]]*\{' || true)
            bundle_ok=$(printf '%s' "$bundle" | grep -c '"mode":"homepage"' || true)
            geo_policy=$(printf '%s' "$headers" | grep -ci '^permissions-policy:.*geolocation=(self)' || true)
            trump_marker=$(printf '%s' "$trump" | grep -c 'data-seo-static-snapshot="build"' || true)
            trump_links=$(printf '%s' "$trump" | grep -Eo 'href="/trump/[^" ]+' | wc -l | tr -d ' ')
            sitemap_jobs=$(printf '%s' "$sitemap" | grep -c '<loc>https://trrb.net/jobs/</loc>' || true)
            sitemap_v7=$(printf '%s' "$sitemap_headers" | grep -ci '^x-trrb-sitemap: live-supabase-v7-jobs-indexable' || true)

            echo "attempt=$attempt sha=$sha_ok homeJobs=$home_jobs falseEmpty=$false_empty jobsIndex=$jobs_index jobsPrelaunch=$jobs_prelaunch jobsApi=$jobs_api_ok bundle=$bundle_ok geo=$geo_policy trump=$trump_marker/$trump_links sitemapJobs=$sitemap_jobs sitemapV7=$sitemap_v7"
            if [ "$sha_ok" -eq 1 ] \
              && [ "$home_jobs" -ge 1 ] && [ "$false_empty" -eq 0 ] \
              && [ "$jobs_index" -ge 1 ] && [ "$jobs_prelaunch" -eq 0 ] \
              && [ "$jobs_api_ok" -ge 1 ] && [ "$bundle_ok" -ge 1 ] \
              && [ "$geo_policy" -ge 1 ] \
              && [ "$trump_marker" -ge 1 ] && [ "$trump_links" -ge 1 ] \
              && [ "$sitemap_jobs" -ge 1 ] && [ "$sitemap_v7" -ge 1 ]; then
              echo 'Production acceptance passed.'
              exit 0
            fi
            sleep 10
          done
          echo 'Production acceptance timed out.'
          exit 1


      - name: 标记生产 SHA 已验收
        if: ${{ success() && env.SKIP_DEPLOY != 'true' }}
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl -fsS -X POST \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            -H 'Content-Type: application/json' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/statuses/$DEPLOY_SHA" \
            -d '{"state":"success","context":"trrb/production-accepted","description":"Production SHA deployed and accepted"}' >/dev/null

      - name: 同一 SHA 已部署时跳过
        if: env.SKIP_DEPLOY == 'true'
        run: echo "Production SHA $DEPLOY_SHA was already deployed and accepted; duplicate run skipped."
