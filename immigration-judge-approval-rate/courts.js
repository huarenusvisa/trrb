const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const fmt=n=>Number(n||0).toLocaleString('zh-CN');const pct=v=>v==null?'—':Number(v).toFixed(1)+'%';let rows=[];function render(list){$('#court-results').innerHTML=list.length?`<div class="crow chead"><span>法院</span><span>法官</span><span>裁决</span><span>批准</span><span>拒绝</span><span>裁决批准率</span></div>${list.map(x=>`<a class="crow" href="/immigration-judge-approval-rate/?q=${encodeURIComponent(x.court_name||x.court_city||'')}"><span><b>${esc(x.court_name||'未命名法院')}</b><small>${esc([x.court_city,x.court_state].filter(Boolean).join(', '))}</small></span><span>${fmt(x.judges)}</span><span>${fmt(x.total_asylum_decisions)}</span><span>${fmt(x.grants)}</span><span>${fmt(x.denials)}</span><span class="rate">${pct(x.adjudicated_approval_rate)}${x.sample_level!=='large'?`<small>${x.sample_level==='small'?'小样本':'中等样本'}</small>`:''}</span></a>`).join('')}`:'<div class="empty">没有找到匹配法院</div>'}async function load(q=''){try{const r=await fetch('/.netlify/functions/immigration-judges?mode=courts'+(q?'&q='+encodeURIComponent(q):''));const d=await r.json();rows=d.courts||[];render(rows);$('#court-count').textContent=fmt(rows.length);$('#court-judges').textContent=fmt(rows.reduce((n,x)=>n+Number(x.judges||0),0));$('#court-decisions').textContent=fmt(rows.reduce((n,x)=>n+Number(x.total_asylum_decisions||0),0))}catch{$('#court-results').innerHTML='<div class="empty"><b>法院数据库暂时无法读取</b><p>请稍后重试。</p></div>'}}$('#court-search').addEventListener('submit',e=>{e.preventDefault();load($('#court-q').value.trim())});load();