const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const fmt=n=>Number(n||0).toLocaleString('zh-CN');const pct=v=>v==null?'—':Number(v).toFixed(1)+'%';let rows=[];function render(list){$('#state-results').innerHTML=list.length?`<div class="crow chead"><span>州</span><span>法院</span><span>法官</span><span>裁决</span><span>批准</span><span>裁决批准率</span></div>${list.map(x=>`<a class="crow" href="/immigration-judge-approval-rate/courts.html?q=${encodeURIComponent(x.state)}"><span><b>${esc(x.state)}</b><small>查看该州法院 →</small></span><span>${fmt(x.courts)}</span><span>${fmt(x.judges)}</span><span>${fmt(x.total_asylum_decisions)}</span><span>${fmt(x.grants)}</span><span class="rate">${pct(x.adjudicated_approval_rate)}${x.sample_level!=='large'?`<small>${x.sample_level==='small'?'小样本':'中等样本'}</small>`:''}</span></a>`).join('')}`:'<div class="empty">没有找到匹配州</div>'}async function load(){try{const r=await fetch('/.netlify/functions/immigration-judges?mode=states');const d=await r.json();rows=d.states||[];render(rows)}catch{$('#state-results').innerHTML='<div class="empty">州级数据库暂时无法读取</div>'}}$('#state-search').addEventListener('submit',e=>{e.preventDefault();const q=$('#state-q').value.trim().toLowerCase();render(rows.filter(x=>String(x.state||'').toLowerCase().includes(q)))});load();