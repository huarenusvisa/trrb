const { rest } = require('./_shared/supabase-admin');
function json(statusCode,body){return{statusCode,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'public, max-age=60, s-maxage=60','X-Content-Type-Options':'nosniff'},body:JSON.stringify(body)}}
exports.handler=async(event)=>{if(event.httpMethod==='OPTIONS')return json(204,{});if(event.httpMethod!=='GET')return json(405,{error:'Method not allowed'});try{const limit=Math.min(Math.max(Number(event.queryStringParameters?.limit||30),1),60);const q=String(event.queryStringParameters?.q||'').trim().slice(0,80);const query={select:'id,slug,primary_name,summary,birth_date,birth_date_precision,death_date,death_date_precision,life_status,us_arrival_date,us_arrival_date_precision,verification_status,updated_at',publication_status:'eq.published',order:'updated_at.desc',limit:String(limit)};if(q)query.primary_name=`ilike.*${q.replace(/[%*,]/g,'')}*`;const rows=await rest('people',{query});return json(200,{source:'people',product:'PEOPLE-R1',items:Array.isArray(rows)?rows:[]})}catch(error){console.error('Public people feed error:',error);return json(error.statusCode||500,{error:error.message||String(error)})}};