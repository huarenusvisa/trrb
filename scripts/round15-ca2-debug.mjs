function ymd(daysAgo){const d=new Date();d.setUTCDate(d.getUTCDate()-daysAgo);return `${d.getUTCFullYear()}${String(d.getUTCMonth()+1).padStart(2,'0')}${String(d.getUTCDate()).padStart(2,'0')}`}
const endpoints=['https://coop.ca2.uscourts.gov/decisions','https://www.ca2.uscourts.gov/decisions','https://ww3.ca2.uscourts.gov/decisions'];for(const url of endpoints){try{const body=new URLSearchParams({IW_DATABASE:'OPN',IW_FIELD_TEXT:'*',IW_FILTER_DATE_AFTER:ymd(30),IW_FILTER_DATE_BEFORE:'',IW_BATCHSIZE:'50',IW_SORT:'-DATE'});const r=await fetch(url,{method:'POST',redirect:'follow',headers:{'user-agent':'Mozilla/5.0 (compatible; TRRB-Legal-Collector-Debug/1.3)','content-type':'application/x-www-form-urlencoded','accept':'text/html,*/*','referer':'https://coop.ca2.uscourts.gov/decisions.html'},body});const text=await r.text();console.log('\nURL',url,'status',r.status,'final',r.url,'type',r.headers.get('content-type'),'len',text.length);console.log('pdfs',text.match(/(?:https?:\/\/[^\s<>'"]+|(?:href|src)\s*=\s*["'][^"']+)[^\n]{0,100}/gi)?.filter(x=>/pdf|opinion|decision/i.test(x)).slice(0,80)||[]);console.log(text.slice(0,2500));}catch(e){console.log('ERR',url,e)}}