const urls=['https://ww3.ca2.uscourts.gov/decisions.html','https://coop.ca2.uscourts.gov/decisions.html'];for(const url of urls){try{const r=await fetch(url,{redirect:'follow',headers:{'user-agent':'Mozilla/5.0 (compatible; TRRB-Legal-Collector-Debug/1.1)','accept':'text/html,*/*'}});const text=await r.text();console.log('\nURL',url,'status',r.status,'final',r.url,'type',r.headers.get('content-type'),'len',text.length);const interesting=text.split(/\r?\n/).filter(line=>/form|action=|script|opinion|decision|search|ajax|php|cgi|recent/i.test(line));console.log(interesting.slice(0,180).join('\n'));}catch(e){console.log('ERR',url,e)}}